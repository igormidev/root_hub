class: MatchChatHistory
table: match_chat_history
fields:
  content: String
  matchSchedulePairingAttemptId: int
  matchSchedulePairingAttempt: MatchSchedulePairingAttempt?, relation(name=pairing_attempt_chat_history, field=matchSchedulePairingAttemptId)
  messages: List<MatchChatMessage>?, relation(name=chat_history_messages)

indexes:
  chat_history_pairing_attempt_id_idx:
    fields: matchSchedulePairingAttemptId
    unique: true
